//! maple-embed — Image → embedding vector via ONNX inference.
//!
//! Phase 0: stub only. Implementation comes in Phase 2.

/// Placeholder: will hold the ONNX session and preprocessing pipeline.
pub struct EmbedEngine;

impl EmbedEngine {
    pub fn new() -> Self {
        tracing::info!("EmbedEngine stub created (phase 0)");
        Self
    }
}
